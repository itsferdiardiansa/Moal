import styled from 'styled-components'
import { COLORS } from '@constant'

export const ButtonContainer = styled.div`
  height: 100%;
  position: relative !important;

  button {
    background-color: ${COLORS['brandNavyPrimary']}
  }
`